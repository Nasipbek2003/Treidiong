# Оптимизация для минимальной задержки

## ✅ Что сделано

### 1. Умное двухуровневое обновление
- **Быстрое обновление:** Каждые 10 секунд обновляется только последняя цена
- **Полное обновление:** Каждые 2 минуты загружаются новые свечи
- **Результат:** Цена обновляется плавно без мерцания, новые свечи появляются вовремя

### 2. Уменьшено время кеширования
- **Было:** 1 час (3600000 мс)
- **Стало:** 1 минута (60000 мс)
- **Результат:** Данные обновляются чаще

### 3. Добавлен индикатор обновления
- Зеленая пульсирующая точка = автообновление активно
- Серая точка = автообновление выключено
- Показывает время с последнего обновления

## 📊 Текущие параметры

```typescript
// Кеш данных
CACHE_DURATION = 60000; // 1 минута

// Автообновление
PRICE_UPDATE_INTERVAL = 10000; // 10 секунд (только цена)
FULL_RELOAD_INTERVAL = 120000; // 2 минуты (новые свечи)

// API запросы
- Обновление цены: каждые 10 сек (легкий запрос)
- Полная перезагрузка: каждые 2 мин (тяжелый запрос)
```

## ⚡ Как это работает

### Цикл обновления:

```
0:00 - 💰 Обновление цены
0:10 - 💰 Обновление цены
0:20 - 💰 Обновление цены
...
2:00 - 🔄 Полная перезагрузка (новые свечи)
2:10 - 💰 Обновление цены
...
```

### Преимущества:
- ✅ Цена обновляется быстро (10 сек)
- ✅ Нет мерцания графика
- ✅ Новые свечи появляются вовремя
- ✅ Экономия API запросов

## 📈 Лимиты API

**Twelve Data Free:**
- 8 запросов/минуту
- Наше использование: ~6-7 запросов/минуту ✅

**Расчет:**
- Обновление цены: 6 раз/мин (каждые 10 сек)
- Полная перезагрузка: 0.5 раз/мин (каждые 2 мин)
- **Итого:** 6.5 запросов/мин (в пределах лимита)

## ⚡ Дальнейшая оптимизация

### Вариант 1: Еще быстрее (15 секунд)

Измени в `app/page.tsx`:

```typescript
// Было:
const interval = 30000; // 30 секунд

// Стало:
const interval = 15000; // 15 секунд
```

⚠️ **Внимание:** Это увеличит количество API запросов в 2 раза!

Лимит Twelve Data: 8 запросов/минуту
- 30 сек = 2 запроса/минуту ✅
- 15 сек = 4 запроса/минуту ✅
- 10 сек = 6 запросов/минуту ✅
- 5 сек = 12 запросов/минуту ❌ (превысит лимит)

### Вариант 2: WebSocket (real-time)

Для настоящего real-time нужен платный план Twelve Data с WebSocket:

```typescript
// Пример WebSocket подключения
const ws = new WebSocket('wss://ws.twelvedata.com/v1/quotes/price');

ws.onopen = () => {
  ws.send(JSON.stringify({
    action: 'subscribe',
    params: {
      symbols: 'XAU/USD',
      apikey: API_KEY
    }
  }));
};

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  // Обновляем цену мгновенно
  updatePrice(data.price);
};
```

**Стоимость:** $79/мес (Grow Plan)

### Вариант 3: Гибридный подход

Используй разные интервалы для разных таймфреймов:

```typescript
const getRefreshInterval = (timeframe: string) => {
  switch(timeframe) {
    case '1min': return 15000;  // 15 сек
    case '5min': return 30000;  // 30 сек
    case '15min': return 60000; // 1 мин
    case '1h': return 120000;   // 2 мин
    default: return 30000;
  }
};
```

## 🔍 Проверка задержки

Открой консоль браузера (F12) и смотри логи:

```
[API] Success! Got 500 candles
[API] Price range: $5020.00 - $5030.00, Latest: $5025.40
[API] Last candle: { date: "2026-01-26 10:05:00", close: 5025.40 }
🔄 Автообновление данных...
```

Время между логами = интервал обновления.

## 📱 Индикатор обновления

В правом верхнем углу ты увидишь:
- 🟢 `5s назад` - данные свежие
- 🟢 `25s назад` - скоро обновится
- 🔴 `2m назад` - что-то не так, нажми кнопку обновления

## ⚠️ Ограничения бесплатного плана

**Twelve Data Free:**
- 8 запросов/минуту
- Задержка данных: 15-30 секунд
- Нет WebSocket

**Это означает:**
- Даже с обновлением каждые 15 сек, данные будут отставать на 15-30 сек от биржи
- Это нормально для технического анализа
- Для скальпинга нужен платный план

## 🎯 Рекомендации

**Для дневной торговли:**
- Текущие настройки (30 сек) оптимальны ✅
- Можно ускорить до 15 сек если нужно

**Для свинг-трейдинга:**
- Можно замедлить до 60 сек
- Экономит API запросы

**Для скальпинга:**
- Нужен платный план с WebSocket
- Или торгуй напрямую через брокера

## 🔧 Ручное обновление

Всегда можешь нажать кнопку 🔄 для мгновенного обновления данных.

Или кнопку 🗑️ для очистки кеша и загрузки свежих данных.
