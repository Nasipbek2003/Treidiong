# Миграция с Binance (крипта) на Twelve Data (золото/форекс)

## ✅ Что изменилось

### Источник данных
- **Было:** Binance API (только криптовалюты)
- **Стало:** Twelve Data API (золото, серебро, форекс, нефть, крипта)

### Символы по умолчанию
- **Было:** BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT
- **Стало:** XAU/USD (золото), XAG/USD (серебро), EUR/USD, GBP/USD

### Формат цен
- **Золото:** ~$5000 (январь 2026)
- **Серебро:** ~$30-35
- **Форекс:** 1.0-1.5 (зависит от пары)

## 🔧 Что нужно сделать

### 1. Добавь API ключ Twelve Data

В `.env.local`:
```bash
NEXT_PUBLIC_TWELVE_DATA_API_KEY=your_key_here
```

Получить бесплатно: https://twelvedata.com/
- Регистрация за 1 минуту
- 8 запросов в минуту (достаточно для 4-8 символов)
- Не требует карты

### 2. Обнови активные символы

Если у тебя были сохранены старые символы (BTCUSDT и т.д.), обнови их:

**Через Telegram бота:**
```
/none          # Отписаться от всех старых
/subscribe XAU/USD   # Подписаться на золото
/subscribe EUR/USD   # Подписаться на евро
/active        # Проверить активные
```

**Или через API:**
```bash
curl -X POST http://localhost:3000/api/signals/preferences \
  -H "Content-Type: application/json" \
  -d '{"activeSymbols": ["XAU/USD", "XAG/USD", "EUR/USD"]}'
```

### 3. Перезапусти систему

```bash
# Останови старый процесс
pm2 stop trading-signals

# Запусти с новыми настройками
npm run signals:start
```

## 📊 Проверка работы

### Тест загрузки данных золота:
```bash
node scripts/test-gold-api.js
```

Должен показать:
```
✓ API Key найден
✓ Получено 10 свечей
💰 Текущая цена золота: $5024.75
✓ Цена в нормальном диапазоне
```

### Тест полной системы:
```bash
node scripts/test-gold-signal.js
```

Должен показать:
```
✓ Получено 100 свечей
💰 Текущая цена: $5024.75
📈 Максимум за 6ч: $5029.66
📉 Минимум за 6ч: $4982.16
📊 RSI(14): 65.3
🎯 Тренд: Восходящий 📈
```

## 🎯 Доступные символы

См. полный список в `lib/signals/SYMBOLS.md`

**Драгоценные металлы:**
- XAU/USD (золото) ✅
- XAG/USD (серебро) ✅

**Форекс:**
- EUR/USD ✅
- GBP/USD ✅
- USD/JPY, USD/CHF, AUD/USD, USD/CAD

**Сырье:**
- CL (нефть WTI)

**Крипта (через Twelve Data):**
- BTC/USD
- ETH/USD

## ⚠️ Важные изменения в коде

### signal-monitor.ts
- `fetchMarketData()` теперь использует Twelve Data API
- Формат интервала: `15min` вместо `15m`
- Timestamp конвертируется из datetime строки

### config.ts
- `AVAILABLE_SYMBOLS` обновлен на форекс/металлы
- `interval` формат изменен на `15min`
- `displayName` на русском

### Формат данных
```typescript
// Было (Binance):
timestamp: 1706270400000  // Unix timestamp
symbol: 'BTCUSDT'

// Стало (Twelve Data):
timestamp: new Date('2026-01-26 10:15:00').getTime()
symbol: 'XAU/USD'
```

## 🐛 Возможные проблемы

### "API credits exceeded"
- Twelve Data лимит: 8 запросов/минуту
- Решение: уменьши количество активных символов или подожди минуту

### "Invalid symbol"
- Проверь формат: `XAU/USD` (не `XAUUSD`)
- Список доступных символов: https://twelvedata.com/docs#supported-symbols

### Старые цены в анализе
- Очисти кеш браузера
- Перезапусти систему мониторинга
- Проверь что используется правильный API ключ

## 📝 Откат на Binance (если нужно)

Если хочешь вернуться на крипту через Binance:

1. В `lib/signals/signal-monitor.ts` замени `fetchMarketData()` на старую версию
2. В `lib/signals/config.ts` верни `BTCUSDT` и т.д.
3. Удали `TWELVE_DATA_API_KEY` из `.env.local`

Но лучше оставить Twelve Data - он поддерживает и крипту тоже! 😉

## ✅ Готово!

Теперь система мониторит золото и форекс с правильными ценами.

Проверь работу:
```bash
npm run signals:start
```

И следи за сигналами в Telegram! 🚀
