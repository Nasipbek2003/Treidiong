# Где ИИ говорит? 🤖

## Простое объяснение:

### 1️⃣ Пользователь нажимает кнопку BUY или SELL

```
[Пользователь] → Нажимает BUY
```

### 2️⃣ Код детектирует паттерны (БЕЗ ИИ)

```typescript
// ЭТО ДЕЛАЕТ КОД (не ИИ):
const pools = detector.getAllPools(candles);
// Результат: [{ type: 'equal_highs', price: 2650.50 }, ...]

const sweeps = sweepDetector.detectSweep(candle, pools);
// Результат: { direction: 'up', wickSize: 0.65, ... }

const validation = validator.validateBreakout(...);
// Результат: { isValid: false, reasons: ['Недостаточный объём'] }
```

### 3️⃣ Код формирует текст (БЕЗ ИИ)

```typescript
// ЭТО ДЕЛАЕТ КОД (не ИИ):
const liquidityText = `
🎯 LIQUIDITY POOLS:
• Equal Highs: 2650.50 (сила: 3)
• PDH: 2660.00

🎯 LIQUIDITY SWEEPS:  ← СЕЙЧАС ЭТО НЕ ДОБАВЛЕНО!
• ⬆️ Sweep на 2655.50 (фитиль: 65%)

⚠️ ЛОЖНЫЙ ПРОБОЙ: 2655.20 невалиден
  • Недостаточный объём
`;
```

### 4️⃣ Текст передаётся ИИ

```typescript
// ЭТО ДЕЛАЕТ КОД (не ИИ):
const systemPrompt = `
Ты профессиональный трейдер...

ДАННЫЕ:
- Цена: 2655.00
- RSI: 68.5

${liquidityText}  ← ВОТ ЗДЕСЬ ИИ ВИДИТ ДАННЫЕ

ПРАВИЛА:
- Equal Highs = зоны стопов
- Sweep = сбор ликвидности
...
`;

// Отправляем ИИ:
const response = await anthropic.messages.create({
  system: systemPrompt,  ← ИИ ПОЛУЧАЕТ ВСЁ ЭТО
  messages: [{ role: 'user', content: message }]
});
```

### 5️⃣ ИИ ГОВОРИТ! 🗣️

```
ИИ читает systemPrompt и видит:
- Цена 2655
- RSI 68.5
- Equal Highs на 2650.50
- Sweep на 2655.50 с фитилём 65%
- Ложный пробой

ИИ отвечает пользователю:
"⚠️ Не входи! Smart Money собрал ликвидность на 2655.50. 
Это stop hunt с фитилём 65%. Жди ретест 2650-2652 для SHORT."
```

## 🎯 Ответ на твой вопрос:

**ИИ говорит ВСЕГДА** когда пользователь нажимает BUY/SELL!

Но теперь:
- **Раньше**: ИИ видел только "Цена 2655, RSI 68.5"
- **Сейчас**: ИИ видит "Цена 2655, RSI 68.5, Equal Highs 2650.50, Sweep 2655.50, Ложный пробой"

## 🔧 Что сейчас работает:

✅ Код детектирует pools → передаёт ИИ → ИИ говорит
✅ Код детектирует sweeps → **НО НЕ передаёт ИИ** ← ПРОБЛЕМА!
✅ Код валидирует пробой → передаёт ИИ → ИИ говорит

## 🎬 Что нужно исправить:

В файле `app/api/chat/route.ts` строка 103, после:
```typescript
${rangeLows.length > 0 ? `• Range Lows: ...` : ''}
```

Добавить:
```typescript
${recentSweeps.length > 0 ? `

🎯 LIQUIDITY SWEEPS (Сбор ликвидности!):
${recentSweeps.map(s => {
  const pool = liquidityPools.find(p => p.id === s.poolId);
  return `• ${s.direction === 'up' ? '⬆️' : '⬇️'} Sweep ${pool?.type} на ${s.sweepPrice.toFixed(2)} (фитиль: ${(s.wickSize * 100).toFixed(0)}%, откат: ${(s.rejectionStrength * 100).toFixed(0)}%)`;
}).join('\n')}
⚠️ Smart Money собрал ликвидность!` : ''}
${breakoutAnalysis}
```

Тогда ИИ будет видеть информацию о sweeps и сможет правильно интерпретировать!

## 📊 Пример диалога:

**Пользователь**: *нажимает BUY*

**Код** (молча):
- Детектирует Equal Highs на 2650.50
- Детектирует Sweep на 2655.50
- Валидирует пробой: ЛОЖНЫЙ

**ИИ** (говорит пользователю):
"⚠️ Не входи! Это stop hunt. Smart Money собрал ликвидность на 2655.50 с фитилём 65%. Жди ретест зоны 2650 для входа."

---

**Вывод**: ИИ говорит всегда, но теперь он **видит больше данных** благодаря коду детекции!
