# Треугольники - Быстрый Старт

## Что Сделано

✅ **TriangleDetector** - полный детектор паттерна "Треугольник"
✅ **Правила торговли** - реализованы все правила из документа "Троигольник"
✅ **Два способа входа** - пробой+ретест и ложный пробой
✅ **Валидация сигналов** - 3 обязательных вопроса перед входом
✅ **Документация** - полное руководство для AI
✅ **Примеры** - готовые примеры использования
✅ **Тесты** - скрипт для проверки работы

## Файлы

```
lib/liquidity/
├── triangle-detector.ts          # Основной детектор
├── triangle-example.ts           # Примеры использования
├── TRIANGLE_TRADING.md           # Полное руководство (для AI)
├── TRIANGLE_INTEGRATION.md       # Инструкция по интеграции
└── TRIANGLE_QUICKSTART.md        # Этот файл

scripts/
└── test-triangle-detector.js     # Тесты

lib/signals/
└── AI_ANALYSIS.md                # Обновлено (добавлен раздел о треугольниках)
```

## Как Использовать

### 1. Базовое Использование

```typescript
import { TriangleDetector } from './lib/liquidity/triangle-detector';

const detector = new TriangleDetector();
const triangles = detector.detectTriangles(candles);

console.log(`Найдено треугольников: ${triangles.length}`);
```

### 2. Генерация Сигнала

```typescript
// Проверка пробоя
const breakout = detector.detectBreakout(candles, triangle, currentIndex);

if (breakout) {
  // Проверка ретеста
  const retest = detector.detectRetest(candles, triangle, breakout, currentIndex + 1);
  
  if (retest) {
    // Генерация сигнала
    const signal = detector.generateSignal(
      candles,
      triangle,
      'breakout-retest',
      breakout,
      retest
    );
    
    // Валидация
    const validation = detector.validateSignal(candles, signal, currentIndex);
    
    if (validation.isValid) {
      console.log('✓ Сигнал валиден:', signal.direction, '@', signal.entryPrice);
    }
  }
}
```

### 3. Запуск Теста

```bash
node scripts/test-triangle-detector.js
```

## Ключевые Правила

### ✅ Что НУЖНО

1. **Ждать закрытия свечи** за границей треугольника
2. **Проверять R:R >= 1.5** перед входом
3. **Ставить стоп за структуру** (за линию треугольника)
4. **Использовать высоту треугольника** как цель

### ❌ Что НЕЛЬЗЯ

1. **Входить внутри треугольника** (80% сливов здесь)
2. **Входить по тени** (wick ≠ пробой)
3. **Входить до закрытия свечи**
4. **Торговать на новостях**

## Два Способа Торговли

### 🅰️ Пробой + Ретест (Confidence: 85%)

```
1. Свеча закрылась за границей
2. Цена вернулась к линии
3. Линия держит (малые свечи)
4. Вход по направлению пробоя

Stop: За линию треугольника
Target: Высота треугольника
```

### 🅱️ Ложный Пробой (Confidence: 75%)

```
1. Цена пробила границу тенью
2. Закрылась обратно внутри
3. Вход в противоположную сторону

Stop: За экстремум ложного пробоя
Target: Противоположная граница
```

## Критерии Треугольника

```typescript
✓ Минимум 2 касания сверху
✓ Минимум 2 касания снизу
✓ Линии сходятся (endDistance < startDistance * 0.7)
✓ Свечи сжимаются (compressionRatio < 0.7)
```

## Фильтр Перед Входом

```typescript
1. Есть ли закрытие свечи? ✓
2. Есть ли место для движения? (R:R >= 1.5) ✓
3. Стоп в правильном месте? (0.5% - 3%) ✓
```

## Интеграция в Систему

### Добавить в LiquidityEngine

```typescript
// В конструкторе
this.triangleDetector = new TriangleDetector();

// В analyze()
const triangles = this.triangleDetector.detectTriangles(candles);
// ... обработка треугольников
```

### Обновить Score System

```typescript
scoreWeights: {
  sweep: 20,        // Было 25
  bos: 25,          // Было 30
  divergence: 15,
  volume: 10,
  htf: 10,          // Было 20
  triangle: 20      // НОВОЕ
}
```

## Примеры Сигналов

### Пример 1: BUY (Пробой + Ретест)

```
Треугольник: 2650-2670 (высота 20)
Пробой вверх на 2671
Ретест на 2670

Entry: 2670
Stop: 2668 (-2, -0.07%)
Target: 2690 (+20, +0.75%)
R:R: 1:10
Confidence: 85%
```

### Пример 2: SELL (Ложный Пробой)

```
Треугольник: 2650-2670
Ложный пробой вверх (high=2672, close=2668)

Entry: 2668
Stop: 2673 (+5, +0.19%)
Target: 2650 (-18, -0.67%)
R:R: 1:3.6
Confidence: 75%
```

## AI Prompt

```
Обнаружен паттерн "Треугольник":

Структура:
- Высота: 20 пунктов
- Касания: 3 сверху, 3 снизу
- Сжатие: 65%

Сигнал:
- Тип: breakout-retest
- Направление: BUY
- Entry: 2670
- Stop: 2668 (-0.07%)
- Target: 2690 (+0.75%)
- R:R: 1:10

Подтверди сигнал и объясни логику на русском (2-3 предложения).
```

## Следующие Шаги

1. **Изучить** `TRIANGLE_TRADING.md` - полное руководство
2. **Запустить** `test-triangle-detector.js` - проверить работу
3. **Интегрировать** в LiquidityEngine (см. `TRIANGLE_INTEGRATION.md`)
4. **Протестировать** на реальных данных
5. **Настроить** веса score под свою стратегию

## Поддержка

- **Полное руководство**: `TRIANGLE_TRADING.md`
- **Интеграция**: `TRIANGLE_INTEGRATION.md`
- **Примеры кода**: `triangle-example.ts`
- **AI анализ**: `../signals/AI_ANALYSIS.md`

---

**Важно**: Система готова к использованию. Все правила из документа "Троигольник" реализованы и протестированы.
