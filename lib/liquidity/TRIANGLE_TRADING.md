# Торговля Треугольниками - Руководство для AI

## Обзор

Треугольник — это паттерн консолидации, где цена сжимается между двумя сходящимися линиями тренда. Правильная торговля треугольниками требует строгой дисциплины и понимания поведения ликвидности.

---

## ШАГ 1: Правильное Построение Треугольника

### Обязательные Условия:

✅ **Минимум 2 касания сверху**
- Должно быть минимум 2 swing high, касающихся верхней линии
- Касания должны быть чёткими, не "натянутыми"

✅ **Минимум 2 касания снизу**
- Должно быть минимум 2 swing low, касающихся нижней линии
- Касания должны быть чёткими, не "натянутыми"

✅ **Линии сходятся**
- Верхняя и нижняя линии должны сходиться к одной точке
- Расстояние между линиями должно уменьшаться минимум на 30%
- Проверка: `endDistance < startDistance * 0.7`

✅ **Цена сжимается (свечи короче)**
- Размер свечей должен уменьшаться со временем
- Средний размер свечей во второй половине < 70% от первой половины
- Проверка: `avgSecondHalf < avgFirstHalf * 0.7`

### Алгоритм Детекции:

```typescript
1. Найти swing highs (локальные максимумы)
   - Цена выше 2 соседних свечей с каждой стороны
   
2. Найти swing lows (локальные минимумы)
   - Цена ниже 2 соседних свечей с каждой стороны
   
3. Построить линии тренда через точки
   - Использовать линейную регрессию
   - Рассчитать slope и intercept
   
4. Проверить сходимость
   - endDistance < startDistance * 0.7
   
5. Проверить сжатие
   - compressionRatio < 0.7
```

### ❌ Что НЕ является треугольником:

- Линии параллельны (это range, не треугольник)
- Линии расходятся (это расширяющийся паттерн)
- Только 1 касание с одной стороны
- Линии "натянуты" через случайные точки
- Свечи не уменьшаются в размере

---

## ШАГ 2: Запрет на Входы (КРИТИЧЕСКИ ВАЖНО)

### 🚫 НЕЛЬЗЯ:

**1. Входить внутри треугольника**
```
Причина: 80% сливов происходит здесь
Решение: Ждать пробоя и подтверждения
```

**2. Входить по тени (wick)**
```
Причина: Тень ≠ пробой
Решение: Ждать закрытия свечи за границей
```

**3. Входить до закрытия свечи**
```
Причина: Цена может вернуться внутрь
Решение: Ждать полного закрытия свечи
```

### Проверка перед входом:

```typescript
// ❌ НЕПРАВИЛЬНО
if (candle.high > upperLine) {
  enterTrade(); // Вход по тени!
}

// ✅ ПРАВИЛЬНО
if (candle.close > upperLine && candleClosed) {
  // Проверяем следующую свечу
  if (nextCandle.close > upperLine * 0.995) {
    enterTrade(); // Вход после подтверждения
  }
}
```

---

## ШАГ 3: Что Считается ПРОБОЕМ

### ✅ Критерии Настоящего Пробоя:

**1. Свеча ЗАКРЫЛАСЬ за границей**
```typescript
// Для пробоя вверх
candle.close > upperLinePrice

// Для пробоя вниз
candle.close < lowerLinePrice
```

**2. Тело свечи за линией**
```typescript
// Не только тень, но и тело
isBodyBreakout = candle.open > upperLinePrice || candle.close > upperLinePrice
```

**3. Следующая свеча не возвращается сразу**
```typescript
// Проверка следующей свечи
nextCandle.close > upperLinePrice * 0.995 // Толерантность 0.5%
```

### ❌ Что НЕ является пробоем:

```typescript
// Только тень пробила
candle.high > upperLine && candle.close < upperLine // ❌

// Свеча не закрылась
candle.high > upperLine && !candleClosed // ❌

// Следующая свеча вернулась
nextCandle.close < upperLine // ❌
```

---

## ШАГ 4: Два Рабочих Способа Торговли

### 🅰️ ВАРИАНТ 1: ПРОБОЙ + РЕТЕСТ (Самый Безопасный)

**Алгоритм:**

```
1. Закрытие свечи за границей
   ✓ candle.close > upperLine (для пробоя вверх)
   ✓ candle.close < lowerLine (для пробоя вниз)

2. Цена возвращается к линии
   ✓ Расстояние до линии < 1%
   ✓ distanceToLine = |candle.close - linePrice| / linePrice < 0.01

3. Линия держит (малые свечи, откат слабый)
   ✓ Для пробоя вверх: candle.low > linePrice * 0.995
   ✓ Для пробоя вниз: candle.high < linePrice * 1.005
   ✓ Размер свечи < 70% от среднего

4. Вход по направлению пробоя
   ✓ BUY если пробой вверх
   ✓ SELL если пробой вниз
```

**Risk Management:**

```typescript
// Для BUY
stopLoss = upperLinePrice * 0.995  // За линию треугольника
takeProfit = entryPrice + triangleHeight  // Высота треугольника

// Для SELL
stopLoss = lowerLinePrice * 1.005  // За линию треугольника
takeProfit = entryPrice - triangleHeight  // Высота треугольника
```

**Пример:**

```
Треугольник: 2650 - 2670
Высота: 20 пунктов

Пробой вверх на 2671
Ретест на 2670 (линия держит)
Entry: 2670
Stop: 2668 (за линию)
Target: 2690 (2670 + 20)
R:R = 1:10
```

**Confidence: 85%** - Самый надёжный метод

---

### 🅱️ ВАРИАНТ 2: ЛОЖНЫЙ ПРОБОЙ (Для Понимания Рынка)

**Алгоритм:**

```
1. Цена вылетает за границу
   ✓ candle.high > upperLine (для ложного пробоя вверх)
   ✓ candle.low < lowerLine (для ложного пробоя вниз)

2. Быстро возвращается внутрь
   ✓ На той же свече или следующей

3. Свеча закрывается ОБРАТНО в треугольнике
   ✓ candle.high > upperLine && candle.close < upperLine
   ✓ candle.low < lowerLine && candle.close > lowerLine

4. Вход в противоположную сторону
   ✓ SELL если ложный пробой вверх
   ✓ BUY если ложный пробой вниз
```

**Risk Management:**

```typescript
// Для BUY (после ложного пробоя вниз)
stopLoss = candle.low * 0.995  // За экстремум ложного пробоя
takeProfit = oppositeLinePrice  // Противоположная граница

// Для SELL (после ложного пробоя вверх)
stopLoss = candle.high * 1.005  // За экстремум ложного пробоя
takeProfit = oppositeLinePrice  // Противоположная граница
```

**Пример:**

```
Треугольник: 2650 - 2670

Ложный пробой вниз:
- Свеча пробила 2650 (low = 2648)
- Закрылась внутри (close = 2652)

Entry: 2652 (BUY)
Stop: 2647 (за экстремум)
Target: 2670 (верхняя граница)
R:R = 1:3.6
```

**Confidence: 75%** - Требует понимания ликвидности

⚠️ **Важно:**
- Используй только если понимаешь ликвидность
- Входи после закрытия свечи
- Не торгуй на новостях

---

## ШАГ 5: Фильтр Перед Входом

### 3 Обязательных Вопроса:

**1️⃣ Есть ли закрытие свечи?**
```typescript
if (!candleClosed) {
  return { isValid: false, reason: 'Нет закрытой свечи' };
}
```

**2️⃣ Есть ли место для движения?**
```typescript
const riskReward = Math.abs(takeProfit - entry) / Math.abs(entry - stopLoss);

if (riskReward < 1.5) {
  return { isValid: false, reason: 'Недостаточно места (R:R < 1.5)' };
}
```

**3️⃣ Где стопы толпы?**
```typescript
const stopDistance = Math.abs(entry - stopLoss) / entry;

if (stopDistance < 0.005) {
  return { isValid: false, reason: 'Стоп слишком близко (< 0.5%)' };
}

if (stopDistance > 0.03) {
  return { isValid: false, reason: 'Стоп слишком далеко (> 3%)' };
}
```

### Если хоть на один ответ "НЕТ" — НЕ ВХОДИ!

---

## Частые Ошибки

### ❌ Ошибка 1: Вход "потому что цена у линии"
```
Проблема: Нет подтверждения пробоя
Решение: Ждать закрытия свечи за границей
```

### ❌ Ошибка 2: Стоп внутри треугольника
```
Проблема: Стоп в зоне консолидации
Решение: Стоп за линию треугольника
```

### ❌ Ошибка 3: Усреднение
```
Проблема: Добавление к убыточной позиции
Решение: Один вход, один стоп
```

### ❌ Ошибка 4: Торговля на новостях
```
Проблема: Непредсказуемая волатильность
Решение: Избегать треугольников во время новостей
```

---

## Чек-Лист для AI

Перед генерацией сигнала проверь:

```typescript
✔️ Треугольник построен правильно
   - Минимум 2 касания сверху
   - Минимум 2 касания снизу
   - Линии сходятся (compressionRatio < 0.7)
   - Свечи сжимаются

✔️ Есть подтверждение пробоя
   - Свеча закрылась за границей
   - Тело свечи за линией
   - Следующая свеча не вернулась

✔️ Есть сигнал на вход
   - Ретест (линия держит, слабые свечи)
   - ИЛИ ложный пробой (закрытие внутри)

✔️ Фильтр пройден
   - Есть закрытие свечи
   - R:R >= 1.5
   - Стоп в правильном месте (0.5% - 3%)

✔️ Risk Management установлен
   - Entry определён
   - Stop за структуру
   - Target = высота треугольника или противоположная граница
```

---

## Примеры Сигналов

### Пример 1: Пробой + Ретест (BUY)

```
Треугольник:
- Верхняя линия: 2670 (нисходящая)
- Нижняя линия: 2650 (восходящая)
- Высота: 20 пунктов
- Касания: 3 сверху, 3 снизу
- Сжатие: 65%

Пробой:
- Свеча закрылась на 2671
- Тело за линией: ✓
- Следующая свеча: 2672 ✓

Ретест:
- Цена вернулась к 2670
- Линия держит (low = 2669.5)
- Слабые свечи (размер 50% от среднего)

Сигнал:
Direction: BUY
Entry: 2670
Stop: 2668 (-2 пункта, -0.07%)
Target: 2690 (+20 пунктов, +0.75%)
R:R: 1:10
Confidence: 85%

Reasoning: "Пробой треугольника вверх с ретестом. Линия держит, 
откат слабый. Вход по направлению пробоя. Цель = высота треугольника."
```

### Пример 2: Ложный Пробой (SELL)

```
Треугольник:
- Верхняя линия: 2670
- Нижняя линия: 2650
- Высота: 20 пунктов

Ложный пробой:
- Свеча пробила вверх (high = 2672)
- Закрылась внутри (close = 2668)
- Быстрый возврат ✓

Сигнал:
Direction: SELL
Entry: 2668
Stop: 2673 (+5 пунктов, +0.19%)
Target: 2650 (-18 пунктов, -0.67%)
R:R: 1:3.6
Confidence: 75%

Reasoning: "Ложный пробой треугольника вверх. Цена вернулась внутрь. 
Вход в противоположную сторону. Цель = нижняя граница треугольника."
```

### Пример 3: Отклонённый Сигнал

```
Треугольник:
- Верхняя линия: 2670
- Нижняя линия: 2665
- Высота: 5 пунктов ❌

Пробой:
- Свеча закрылась на 2671

Проблема:
- R:R = 5 / 2 = 2.5 / 2 = 1.25 ❌
- Недостаточно места для движения

Результат: СИГНАЛ ОТКЛОНЁН
Reason: "Недостаточно места для движения (R:R = 1.25 < 1.5)"
```

---

## Интеграция с Существующей Системой

### Добавление в LiquidityEngine:

```typescript
// В analyze()
const triangles = this.triangleDetector.detectTriangles(candles);

for (const triangle of triangles) {
  // Проверка пробоя
  const breakout = this.triangleDetector.detectBreakout(
    candles, 
    triangle, 
    currentIndex
  );
  
  if (breakout) {
    // Проверка ретеста
    const retest = this.triangleDetector.detectRetest(
      candles,
      triangle,
      breakout,
      currentIndex
    );
    
    if (retest) {
      const signal = this.triangleDetector.generateSignal(
        candles,
        triangle,
        'breakout-retest',
        breakout,
        retest
      );
      
      if (signal) {
        const validation = this.triangleDetector.validateSignal(
          candles,
          signal,
          currentIndex
        );
        
        if (validation.isValid) {
          // Добавить к сигналам
          signals.push(signal);
        }
      }
    }
  }
  
  // Проверка ложного пробоя
  const falseBreakout = this.triangleDetector.detectFalseBreakout(
    candles,
    triangle,
    currentIndex
  );
  
  if (falseBreakout) {
    const signal = this.triangleDetector.generateSignal(
      candles,
      triangle,
      'false-breakout',
      undefined,
      undefined,
      falseBreakout
    );
    
    if (signal) {
      const validation = this.triangleDetector.validateSignal(
        candles,
        signal,
        currentIndex
      );
      
      if (validation.isValid) {
        signals.push(signal);
      }
    }
  }
}
```

### Добавление в Score System:

```typescript
// В SignalScorer
triangleScore: number; // 0-20 баллов

// Критерии:
// - Качество треугольника (сжатие, касания): 0-10
// - Тип сигнала (ретест = 10, ложный = 7): 0-10

calculateTriangleScore(signal: TriangleSignal): number {
  let score = 0;
  
  // Качество треугольника
  score += signal.triangle.compressionRatio * 10;
  
  // Тип сигнала
  if (signal.type === 'breakout-retest') {
    score += 10;
  } else if (signal.type === 'false-breakout') {
    score += 7;
  }
  
  return Math.min(score, 20);
}
```

---

## Заключение

Торговля треугольниками требует:
1. **Терпения** - ждать правильного сетапа
2. **Дисциплины** - следовать правилам входа
3. **Понимания** - знать где стопы толпы

AI должен генерировать сигналы только когда ВСЕ критерии выполнены.

**Помни:** 80% сливов происходит из-за преждевременных входов. Лучше пропустить сигнал, чем войти неправильно.
